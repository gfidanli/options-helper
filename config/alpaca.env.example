# Alpaca credentials/config (DO NOT COMMIT real secrets)
#
# Usage:
#   cp config/alpaca.env.example config/alpaca.env
#   then fill in values below.
#
# This repo will auto-load `config/alpaca.env` and `.env` at runtime (only keys
# starting with APCA_ or OH_ALPACA_). Existing environment variables win.
#
# Prefer SIP if your plan supports it (higher quality than IEX).

APCA_API_KEY_ID="YOUR_KEY_ID"
APCA_API_SECRET_KEY="YOUR_SECRET_KEY"

# Optional:
# APCA_API_BASE_URL="https://paper-api.alpaca.markets"
OH_ALPACA_STOCK_FEED="sip"
# OH_ALPACA_OPTIONS_FEED="opra"
# OH_ALPACA_RECENT_BARS_BUFFER_MINUTES="16"
